apiVersion: v1
kind: Service
metadata:
  name: aoe2companion-refetch
  namespace: default
  labels:
    app: aoe2companion-refetch
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: aoe2companion-refetch