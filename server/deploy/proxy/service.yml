apiVersion: v1
kind: Service
metadata:
  name: aoe2companion-proxy
  namespace: default
  labels:
    app: aoe2companion-proxy
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: aoe2companion-proxy