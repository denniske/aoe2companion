apiVersion: v1
kind: Service
metadata:
  name: aoe2companion-ingest-fast
  namespace: default
  labels:
    app: aoe2companion-ingest-fast
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: aoe2companion-ingest-fast