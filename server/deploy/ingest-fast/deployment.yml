apiVersion: apps/v1
kind: Deployment
metadata:
  name: aoe2companion-ingest-fast
  namespace: default
  labels:
    app: aoe2companion-ingest-fast
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aoe2companion-ingest-fast
  template:
    metadata:
      labels:
        app: aoe2companion-ingest-fast
    spec:
      containers:
        - name: aoe2companion-ingest-fast
          image: denniske/aoe2companion-ingest-fast:$COMMIT_SHA1
          ports:
            - containerPort: 80
              name: http
          envFrom:
            - secretRef:
                name: server-secret
          env:
            - name: PORT
              value: "80"
            - name: K8S_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name