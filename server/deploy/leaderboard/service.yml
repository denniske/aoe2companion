apiVersion: v1
kind: Service
metadata:
  name: aoe2companion-function
  namespace: default
  labels:
    app: aoe2companion-function
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: aoe2companion-function