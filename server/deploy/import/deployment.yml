apiVersion: apps/v1
kind: Deployment
metadata:
  name: aoe2companion-import
  namespace: default
  labels:
    app: aoe2companion-import
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aoe2companion-import
  template:
    metadata:
      labels:
        app: aoe2companion-import
    spec:
      containers:
        - name: aoe2companion-import
          image: denniske/aoe2companion-import:$COMMIT_SHA1
          ports:
            - containerPort: 80
              name: http
          envFrom:
            - secretRef:
                name: server-secret
          env:
            - name: PORT
              value: "80"
            - name: K8S_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name